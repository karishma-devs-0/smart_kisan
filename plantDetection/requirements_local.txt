# Local GPU training requirements (WSL2 + Ubuntu + NVIDIA GPU)
# Install with: pip install -r requirements_local.txt

# TensorFlow with bundled CUDA + cuDNN (latest stable that supports Blackwell GPUs)
tensorflow[and-cuda]>=2.16.0

# Data + ML
numpy>=1.26.0
scikit-learn>=1.3.0
pillow>=10.0.0

# Visualization (optional but used by the notebook for plots)
matplotlib>=3.7.0
seaborn>=0.13.0

# Notebook support (skip if running train_model.py only)
jupyterlab>=4.0.0
ipykernel>=6.0.0
